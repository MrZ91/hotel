<div class="col-md-2 ">
  <ul class="nav nav-pills nav-stacked">
    <li <%='class=active'if current_page?(root_url)%>><%=link_to("Main",root_url)%></li>
    <li
        <% if user_signed_in? %>
          <%='class=active'if current_page?(current_user)%>><%=link_to("Profile",current_user)%>
        <%else%>
          <%='class=active'if current_page?(new_user_session_path)%>><%=link_to("Sign in",new_user_session_path)%>
        <%end%>
    </li>
    <li <%='class=active'if current_page?('#')%>><%=link_to("Search",'#')%></li>
    <%= debug(params) if Rails.env.development? %>
    <%= "current user id:#{current_user.id}" if user_signed_in? %>
  </ul>
</div>